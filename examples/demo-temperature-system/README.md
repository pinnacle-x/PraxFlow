# Demo Temperature System

Synthetic example only.

Product contexts:

```text
PC
Main Controller
Temperature Module
Shared Protocol
Manifest
```

A representative workspace would be:

```text
demo-temperature-system/
├── system/
├── protocol/
├── pc/
├── controller/
├── module-temperature/
└── manifest/
```

The example files show selected outputs rather than a complete generated project. They are reference fixtures for the expected shape of `CONTEXT-MAP.md`, `OPEN_DECISIONS.md`, protocol data, and a release manifest.
