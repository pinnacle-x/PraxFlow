# Exported Documents

This directory stores publication snapshots generated from the maintained Markdown process documentation.

Rules:

- Markdown under `docs/` is the editing source of truth.
- Export filenames may contain explicit versions such as `V1.0`.
- Do not hand-edit an export and leave Markdown stale.
- When workflow behavior changes, update Markdown first, then regenerate the export.

Current export:

- `PraxFlow_MultiRepo_Product_Engineering_Workflow_V1.0.docx`
